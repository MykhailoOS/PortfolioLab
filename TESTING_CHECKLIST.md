# Testing Checklist для Supabase Migration

## ✅ Тесты для проверки

### 1. Аутентификация

- [ ] Регистрация нового пользователя
- [ ] Login с существующим пользователем
- [ ] Logout
- [ ] Session persist после перезагрузки страницы

### 2. Список проектов

- [ ] Создать новый проект (кнопка "New Project")
- [ ] Проекты отображаются в списке
- [ ] Открыть проект (кнопка "Open")
- [ ] Переименовать проект (иконка Edit)
- [ ] Удалить проект (иконка Trash)

### 3. Редактор проекта

- [ ] Открыть редактор (/editor/:projectId)
- [ ] Добавить новую секцию (Library → Add Section)
- [ ] Редактировать данные секции (Inspector)
- [ ] Drag & drop reorder секций
- [ ] Autosave работает (смотри индикатор "Saved" в TopBar)

### 4. Persistence (сохранение)

- [ ] Изменить данные секции
- [ ] Закрыть проект
- [ ] Открыть снова
- [ ] Данные сохранились ✅

### 5. Ошибки

- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в терминале (npm run dev)
- [ ] Network requests успешны (проверь DevTools → Network)

## 🐛 Known Issues to Watch

### Возможные проблемы:

1. **404 на autosave** - если увидишь, проверь что `_strapiDocumentId` правильно установлен
2. **Infinite loop autosave** - не должно быть, но следи за количеством запросов
3. **RLS policy errors** - если видишь "permission denied", проверь что пользователь залогинен

## 📊 Проверка в Supabase Dashboard

### Зайди в Supabase Dashboard:

1. Перейди на https://supabase.com/dashboard/project/aidubaycwyuibtmdmsnr
2. Table Editor → `projects`
   - Должны быть твои проекты
   - `user_id` должен совпадать с твоим auth.uid
3. Table Editor → `blocks`
   - Должны быть блоки твоих проектов
   - `project_id` должен ссылаться на существующий проект
   - `data` должен содержать `{ sectionData: {...}, effects: {...} }`
4. Table Editor → `pages`
   - Должна быть хотя бы одна страница для каждого проекта

## 🎯 Acceptance Criteria

Migration считается успешной, если:

- ✅ Можно создать проект
- ✅ Можно добавить и редактировать секции
- ✅ Autosave работает без ошибок
- ✅ Данные сохраняются и загружаются корректно
- ✅ Можно удалить проект
- ✅ Нет ошибок в консоли

## 🚀 Следующие шаги после тестирования

Если все тесты пройдены:

1. **Создай .env файл** с Supabase credentials
2. **Обнови lib/supabase.ts** чтобы читать из env
3. **Удали Strapi backend**: `rm -rf portfolio-backend`
4. **Удали старый API**: `rm services/strapiApi.ts`
5. **Создай vercel.json** для SPA routing
6. **Deploy на Vercel**: `vercel`

---

**Текущий статус**: Dev server запущен ✅
**URL**: http://localhost:5173 (или другой порт)
**Следующее действие**: Открой браузер и протестируй!
