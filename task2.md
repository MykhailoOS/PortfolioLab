# 🎯 TASK 2 — STRICT PROMPT
**Title:** Главный Dashboard + система проектов (создание/управление/сохранение) + персистентность проекта в конструкторе  
**Backend:** **Strapi** (используем уже развернутый), **REST** API  
**Frontend:** существующий **React**‑клиент и текущие стили/компоненты (без новых UI‑библиотек)

---

## 0) Цель (что должно появиться)
1) **Главный Dashboard** c разделом **Projects**: список, создание, переименование, архивирование/удаление, переход в конструктор.  
2) **CRUD проектов/страниц/блоков** на бэкенде **Strapi** (минимальные коллекции и права).  
3) **Сохранение проекта в конструкторе**: автосохранение (debounce), загрузка при входе, устойчивость к конфликтам версий.

---

## 1) Ограничения (жёстко)
- **Никаких новых UI‑библиотек и редизайна.** Используем существующие компоненты/стили.  
- **Структура приложения** (Library / Canvas / Inspector) остаётся прежней; добавляем только Dashboard и сервисы API.  
- **Авторизация** на время демо — **Strapi API Token** (role‑scoped), без пользовательской регистрации.  
- **Медиа** уже в Strapi Upload — **не менять**; ссылки вида `/uploads`/`/assets/:id` сохраняются в блоках.

---

## 2) Модель данных (Strapi · минимально жизнеспособная)

Создать три коллекции (Collection types):

- `project`  
  Поля:  
  - `name` (string, required)  
  - `slug` (UID, unique)  
  - `defaultLocale` (string, default `en`)  
  - `enabledLocales` (JSON, массив строк)  
  - `theme` (JSON, опционально)  
  - `status` (enum: `active` | `archived`, default `active`)

- `page` (many‑to‑one → project)  
  Поля:  
  - `project` (relation)  
  - `path` (string, например `/`)  
  - `order` (integer)  
  - `seo` (JSON)

- `block` (many‑to‑one → page)  
  Поля:  
  - `page` (relation)  
  - `type` (string: `about` | `projects` | `hero` | `contact` …)  
  - `order` (integer)  
  - `data` (JSON)   ← содержимое блока  
  - `style` (JSON)  ← box‑model/align и т.п.  
  - `effects` (JSON)← флаги эффектов (если есть)

**Права/Роли:**  
- Создать роль (например, `portfolio-editor`) с доступом к `project/page/block`: `find`, `findOne`, `create`, `update`, `delete`.  
- Сгенерировать **API Token** (scoped).

**CORS:** разрешить домен клиента.

---

## 3) Контракты API (REST, минимальный набор)

- **Projects**
  - `GET /api/projects?filters[status][$ne]=archived&sort=updatedAt:desc&pagination[pageSize]=50`
  - `POST /api/projects` → `{ name, slug, defaultLocale, enabledLocales }`
  - `PUT /api/projects/:id` → `{ name?, status? }`
  - `DELETE /api/projects/:id` (или `status: archived` через PUT)

- **Pages**
  - `GET /api/projects/:id/pages?sort=order:asc`
  - `POST /api/pages` → `{ project, path, order, seo }`
  - `PUT /api/pages/:id` → `{ path?, order?, seo? }`
  - `DELETE /api/pages/:id`

- **Blocks**
  - `GET /api/pages/:id/blocks?sort=order:asc`
  - `POST /api/blocks` → `{ page, type, order, data, style, effects }`
  - `PUT /api/blocks/:id` → частичные обновления (`data/style/effects/order`)
  - `DELETE /api/blocks/:id`

**Загрузка дерева проекта (для конструктора):**  
- Один запрос `GET /api/projects/:id?populate[pages][populate][blocks]=*` (или два: pages и blocks отдельно, если дерево слишком тяжёлое).

**Заголовки:** `Authorization: Bearer <STRAPI_API_TOKEN>`

---

## 4) Dashboard (UX · без новых UI)

**Роуты фронта:**
- `/dashboard` → редирект на `/dashboard/projects`
- `/dashboard/projects` (список проектов)
- `/editor/:projectId` (конструктор конкретного проекта)

**Экраны/операции:**
- **Projects List**
  - Карточки/строки: `name`, `updatedAt`, `status`, actions: **Open**, **Rename**, **Archive**, **Delete** (если допущено).
  - **Create Project** (модалка): `name` (required), `slug` (auto), `defaultLocale`, `enabledLocales[]`.
- **Редирект в конструктор:** кнопка **Open** → `/editor/:projectId`.

**Требования по UX:**
- Спиннер/скелетоны при загрузке списков.  
- Подтверждение на Archive/Delete.  
- Сообщения об ошибках (toast/inline) — использовать существующие компоненты.

---

## 5) Конструктор: загрузка/сохранение (персистентность)

**Инициализация:**
- При заходе на `/editor/:projectId`:  
  1) запросить `project` (+ `pages` + `blocks`),  
  2) распаковать в стор редактора (структура дерева),  
  3) отрисовать Canvas.

**Автосохранение (debounce 500–1000 мс):**
- При изменении блока отправлять **минимальный PATCH** на `PUT /api/blocks/:id`.  
- При `add/remove/reorder` — синхронизировать `order`/`page` через соответствующие эндпоинты.  
- Показ статуса: **Saving… / Saved / Error** (использовать текущие стили).

**Конфликты версий (базово):**
- Перед `PUT` сверять `updatedAt` (или версию).  
- Если сервер вернул ошибку конфликта/несовпадение `updatedAt` — показать предупреждение: «Версия изменилась на сервере», варианты: **Reload** / **Overwrite** (если политика позволяет). На старте — предложить **Reload**.

**Стабильность ID:**
- После `POST /api/blocks` и `POST /api/pages` — сразу подменять временные id в сторе на id с сервера.

---

## 6) Валидации (минимум)

- **Фронт:**  
  - `project.name` — required.  
  - В блоках: обязательные поля (`title`, `paragraph`…), `alt` если есть `image`.  
- **Бэк (Strapi):**  
  - Ограничения размеров и типов файлов уже действуют.  
  - Для JSON‑полей — базовые проверки (не пустые строки для required).

---

## 7) Производительность

- Дебаунс отправок; батчить несколько изменений одного блока в один запрос, если они произошли в короткий интервал.  
- Не перерисовывать весь Canvas при точечных изменениях (мемо/селекторы).  
- Пагинация/ограничение списка проектов (например, 50 на страницу).

---

## 8) Переменные окружения (фронт)

- `VITE_STRAPI_URL` — базовый URL Strapi.  
- `VITE_STRAPI_TOKEN` — API‑токен роли `portfolio-editor`.

---

## 9) Acceptance Criteria

1. **Dashboard Projects** показывает список проектов из Strapi, поддерживает **Create / Rename / Archive / Delete** (в рамках прав).  
2. Кнопка **Open** открывает `/editor/:projectId`, редактор подтягивает **project→pages→blocks** и отрисовывает.  
3. **Автосохранение** работает: правки блока отправляются патчами, индикатор статуса виден, после перезагрузки данные сохраняются.  
4. **Add/Remove/Reorder** блоков отражаются в Strapi (корректные `order`), Canvas синхронизирован.  
5. При конфликте версий пользователь видит предупреждение и может **перезагрузить** данные проекта.  
6. **Без новых UI‑зависимостей**, существующий дизайн/стили сохранены, в консоли нет ошибок.

---

## 10) Deliverables (один PR)

- Конфигурация Strapi: коллекции `project`, `page`, `block`, права роли, короткая документация.  
- Серверные/клиентские сервисы API: методы **Projects/Pages/Blocks**.  
- Экраны Dashboard `/dashboard/projects` и маршрут `/editor/:projectId`.  
- Реализованное **autosave** (debounce), CRUD для блоков и порядок.  
- Обновлённый `README` по запуску Strapi, настройке токена и переменных окружения.

---

Когда будешь готов — напиши **«next»**, и пришлю **TASK 2.2**: _Layout/Spacing/Effects_ (box‑model, align, blur/parallax‑флаги) с сохранением в `block.style`/`block.effects` и применением на Canvas — тоже **без новых UI‑библиотек**.